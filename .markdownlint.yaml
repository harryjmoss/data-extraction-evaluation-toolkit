---
# Markdown linting rules to ignore.
MD013: false #  line-length
ignore:
  - "tests/test_files/**/*"
MD046: false # code block style (need both styles if tabs are to work)
MD024: # allow duplicated headings as long as they are not siblings (for changelog)
  siblings_only: true
